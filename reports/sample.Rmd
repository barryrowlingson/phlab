---
output:
 md_document:
  toc: false
 pdf_document:
  toc: false
text: md
---

```{r docopt}
library(docopt)
'usage: sample.Rmd [-a -r -m <msg>]

options:
 -a        Add
 -r        Remote
 -m <msg>  Message' -> doc
opts <- docopt(doc, argv)
print(opts)

message("a is ",opts$a)

```

# Sample report

This is a sample markdown report.

The parameters are

```{r args}
print(argv)
```

```{r test}
samp = data.frame(
     X = runif(10),
     Y = runif(10))
m = lm(Y~X, data=samp)
summary(m)
```

That's that.


